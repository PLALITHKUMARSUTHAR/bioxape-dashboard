{
  "name": "bioxape-dashboard-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
